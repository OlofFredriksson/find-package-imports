const apiClient = require("@forsakringskassan/api-client");
const db = require("@forsakringskassan/database");

const app = apiClient.createApp();
db.connect("mongodb://localhost/mydb");

app.get("/", (req, res) => {
    res.send("Hello from Package C");
});

app.listen(3000);
