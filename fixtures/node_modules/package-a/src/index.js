import logger from "@forsakringskassan/logger";
import utils from "@forsakringskassan/utils";

export function processData(data) {
    const filtered = utils.filter(data, (item) => item.active);
    logger.info("Processing data");
    return filtered;
}
